$file = "index.ts"
$lines = Get-Content $file
$first = $lines[0..499]
$last = $lines[-15..-1]
$first += "// ---------------------------------------------------------------------------"
$first += "// Start"
$first += "// ---------------------------------------------------------------------------"
$first += "// ---------------------------------------------------------------------------"
$first += $last
Set-Content $file -Value $first